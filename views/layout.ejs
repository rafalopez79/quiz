<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Quiz</title>
    <meta charset="utf-8"/>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/wide.css' media='only screen and (min-width: 530px) and (min-device-width: 481px)'/>
    <link rel='stylesheet' href='/stylesheets/smartphone.css' media='only screen and (max-width: 480px)'/>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
  </head>
  <body>
    <div id="page-wrap">
        <header class="main" id="h1"/>
            <% if (!session.user) { %>
                <span class='right'><a href='/login'>Login</a></span>
            <% } else { %>
                <span class='right'><%=session.user.username%><a href='/logout'>Logout</a></span>
            <% } %>
            <h2> Quiz: el juego de las preguntas </h2>
        </header>
        <nav class="main" id="n1" role="navigation">
            <span><a href="/">Inicio</a></span>
            <span><a href="/quizes">Preguntas</a></span>
            <span><a href="/author">Cr&eacute;ditos</a></span>
            <% if (session.user){ %> 
                <span><a href="/quizes/statistics">Estad&iacute;sticas</a></span>
            <% } %>
        </nav>
        <section class="main" id="s1">
            <% if (errors.length) { %>
              <span id='ErrorMsgs'>
                Corregir errores:<br/>
                <% for (var i in errors) { %>
                   <span> <%= errors[i].message %> </span><br/>
                <% } %>
              </span>
            <% } %>
            <%- body %>
        </section>
        <footer class="main" id="f1">
            <p><a href="http://es.wikipedia.org/wiki/Quiz"> Que es un quiz? </a></p>
            <p><a href="https://github.com/rafalopez79/quiz"> Source code</a></p>
        </footer>
    </div>    
  </body>
</html>
